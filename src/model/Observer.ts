import { LatLng } from "./LatLng";

export class Observer implements LatLng {
  latitude: number = 0;
  longitude: number = 0;

  // setPosition(position: LatLng): void {
  //   this.latitude = position.latitude;
  //   this.longitude = position.longitude;
  // }
}
